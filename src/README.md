# Source Code

Put reusable project code here, such as:

- data acquisition;
- schema validation and cleaning;
- feature construction;
- model training and prediction;
- metric calculation;
- plotting helpers; and
- reproducibility checks.

Keep configuration explicit, use clear function names, and add tests for transformations that could change the main result. Avoid copying the same logic across notebooks.
